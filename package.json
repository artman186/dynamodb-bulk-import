{
  "name": "dynamodb-bulk-import",
  "version": "1.0.0",
  "description": "Tool for importing DynamoDB files exported to S3.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1050.0",
    "csv-writer": "^1.6.0"
  }
}
